name: Keep Balance Sim Awake

on:
  schedule:
    - cron: '0 */4 * * *'  # 4시간마다 실행
  workflow_dispatch:  # 수동 실행 버튼

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
    - name: Wake App
      uses: zzzze/action-uptime@v1
      with:
        url: https://gartist082-guardian-verifier.streamlit.app
